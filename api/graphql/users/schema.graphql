type Account {
  username: String!
  password: String!
  userid: Int!
}

type Query {
  login(email: String!, password: String!): String!
  tfa(value: String!, user_id: Int!): String!
}

type Mutation {
  signup(email: String!, username: String!, password: String!): Account!
}
